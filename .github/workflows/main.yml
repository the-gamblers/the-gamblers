name: iOS Build

on:
  push:
    branches: "main"

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build iOS app
      run: |
        xcodebuild clean build -workspace ChessMaster.xcworkspace -scheme 'ChessMaster' -destination 'platform=iOS Simulator,name=iPhone 15'
